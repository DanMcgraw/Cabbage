//! Player-facing presentation: bossbars now, protected menus only once the
//! native plugin GUI lifecycle is confirmed sufficient (see plan Phase 4).

pub(crate) mod bossbar;

pub(crate) use bossbar::BossbarState;
